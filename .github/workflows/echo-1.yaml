name: echo-2

on:
  push:
    tags:
      - 'v*'
    
jobs:
  echo-1:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: prod
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo
        run: |
          echo "Hello, world 1!"
