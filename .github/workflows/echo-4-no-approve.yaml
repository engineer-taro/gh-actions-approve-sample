name: echo-3

on:
  push:
    tags:
      - 'v*'
    
jobs:
  echo-4-no-approve:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo
        run: |
          echo "Hello, world 4!"
